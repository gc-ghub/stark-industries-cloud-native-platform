# NOTE: .helmignore files are plain text (glob patterns) not YAML — treat as plaintext.
# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml
npm-debug.log

# Helm packaging artifacts
*.tgz
*.tar.gz
charts/

# Temporary / test directories
README.md
tests/
test/

# Docker artifacts
Dockerfile
docker-compose.yml
